[package]
name = "coreml-bridge"
version = "0.1.0"
edition = "2021"
description = "Rust → Core ML predict bridge for ANE-routed vision encoders."
license = "MIT OR Apache-2.0"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
cc = "1"
